# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
auth/src/tool/authentication-api-cli/authentication-api-cli
auth/src/auth
.vscode/launch.json
